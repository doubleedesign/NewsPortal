<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_articleItem"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!--The article image-->
    <ImageView
        android:id="@+id/imageView_articleImage"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_marginBottom="5dp"
        />

    <!--The article heading-->
    <TextView
        android:id="@+id/textView_articleTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/eb_garamond"
        android:textColor="#FFF"
        android:textSize="20sp"
        android:paddingStart="15dp"
        android:paddingEnd="15dp"
        />

    <!--The URL (hidden field)-->
    <TextView
        android:id="@+id/textView_articleURL"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        />

    <!--The NewsAPI.org credit-->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="15dp"
        android:paddingStart="15dp"
        android:paddingEnd="15dp"
        android:layout_marginTop="20dp"
        android:text="Powered by NewsAPI.org"
        android:textSize="12sp"
        android:textColor="#999"
        />

</LinearLayout>